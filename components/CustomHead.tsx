import Head from 'next/head'

const CustomHead = () => {
    return (
        <Head>
            <title>Testing Postgre commands</title>
            <meta name="description" content="postgre testing app" />
            <link rel="icon" href="/favicon.ico" />
        </Head>
    );
};

export default CustomHead;